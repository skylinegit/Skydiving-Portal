# Skyline Skydiving Portal — backend env template
#
# Copy this file to backend/.env and fill in the real values.
# backend/.env is gitignored and must NEVER be committed.

# -----------------------------------------------------------------------------
# Database
# -----------------------------------------------------------------------------
# DigitalOcean Managed Postgres connection string.
# SSL is required by the managed cluster — keep ?sslmode=require.
DATABASE_URL=postgresql://USER:PASSWORD@HOST:25060/defaultdb?sslmode=require

# -----------------------------------------------------------------------------
# App
# -----------------------------------------------------------------------------
APP_ENV=development          # development | staging | production
SESSION_COOKIE_NAME=skyline_session
SESSION_TTL_DAYS=30
PASSWORD_RESET_TTL_MINUTES=30
EMAIL_CHANGE_TTL_MINUTES=60

# -----------------------------------------------------------------------------
# N8N webhook endpoints (CRM bridge)
# -----------------------------------------------------------------------------
N8N_BASE_URL=https://n8n.skylineevents.co.uk
N8N_WEBHOOK_TOKEN=replace-me

# -----------------------------------------------------------------------------
# Email (transactional)
# -----------------------------------------------------------------------------
SMTP_HOST=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM=no-reply@skylineevents.co.uk
